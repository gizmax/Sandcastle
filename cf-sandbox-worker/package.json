{
  "name": "sandcastle-cf-sandbox-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that wraps CF Sandbox for Sandcastle agent execution",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@cloudflare/sandbox": "^0.1.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0",
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
