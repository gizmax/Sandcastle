# X post — Sandcastle 0.47

## Hlavní post

> Everyone ships agents that finish. Nobody ships agents that succeed.
>
> Sandcastle 0.47: a gate that judges the work and stops the run — with a
> written evidence pack. A replay that never pays twice. A crashed run that
> finishes itself. Memory a regulator can read, with a grade.
>
> Approve it once. Leave the room.
>
> sandcastle-ai.eu/whatsnew/

## Alternativa — thread (4 posty)

**1/**
> Kill your worker at step 40 of 60.
>
> In Sandcastle 0.47, the run finishes anyway. Steps 1–40 replay from the
> effect ledger: $0, zero duplicate sends. The invoice does not go out twice.
>
> That's resume, not retry.

**2/**
> New: `type: accept`. A worker produces the work, a judge panel decides if it
> met the goal. Free deterministic checks first, then N-of-M models, then a
> human if they disagree.
>
> Rejection stops the run and writes an evidence pack into the audit chain.

**3/**
> `sandcastle memory eval` grades your agent's memory: staleness, forgetting,
> contradictions — including the immortal memories it will never re-examine.
>
> Storing isn't remembering. Now there's a number that knows the difference.

**4/**
> Plus: any of ~38 ACP harnesses (Claude Code, Codex, goose) as a worker in
> your DAG. Markdown memory with zero dependencies. A sweep that catches runs
> claiming more than they can prove.
>
> 0.47 "Leave the Room" — sandcastle-ai.eu
